/**
 * Custom Initialization for MarioCube
 * Materialize components are initialized via M.AutoInit() in footer.html.
 * Add project-specific JS logic here.
 */
document.addEventListener('DOMContentLoaded', function() {
  // M.AutoInit() is already called in footer.html
  // Add any custom component overrides or additional logic here
});
